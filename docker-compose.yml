services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: olkan
      POSTGRES_USER: olkan
      POSTGRES_PASSWORD: olkan_pass
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    restart: unless-stopped

volumes:
  postgres_data:
